import 'package:flutter/material.dart';

class DetailPlg extends StatelessWidget {
  const DetailPlg({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}